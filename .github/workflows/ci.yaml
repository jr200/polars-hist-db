name: ci

on:
  pull_request:
    branches: [master]
  push:
    tags:
      - v*.*.*

jobs:
  ci:
    uses: jr200-labs/github-action-templates/.github/workflows/ci_uv_python.yaml@master
    with:
      run-tests: false

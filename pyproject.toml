[project]
name = "polars-hist-db"
version = "0.12.80"
description = "(dsv,jetstream) --> dataframe <--> bitemporal-tables"
authors = [{ name = "jr200", email = "jayshan+git@gmail.com" }]
readme = "README.md"
requires-python = ">=3.12,<4.0"

dependencies = [
    "nats-py>=2.15.0",
    "polars>=1.43.0",
    "pyarrow>=25.0.1",
    "pymysql>=1.2.0",
    "pytz>=2026.3.post1",
    "pyyaml>=6.0.3",
    "scandir-rs>=2.9.9",
    "sql-metadata>=3.0.1",
    "sqlalchemy[asyncio]>=2.0.52",
]

[project.optional-dependencies]
sqlalchemy = ["polars"]
nats = ["nats-py"]
xtdb = ["adbc-driver-flightsql>=1.12.0", "psycopg[binary]>=3.3.4"]
crdt = [
    "pycrdt>=0.14.2",
]

[project.urls]
Repository = "https://github.com/jr200-labs/polars-hist-db"

[project.scripts]
polars-hist-db-parity = "polars_hist_db.parity:main"

[dependency-groups]
dev = [
    "coverage>=7.15.4",
    "ipykernel>=7.3.0",
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "quarto-cli",
    "ruff>=0.16.3,<0.17",
    "types-python-dateutil>=2.9.0.20260807",
    "types-pytz>=2026.3.1.20260727",
    "types-pyyaml>=6.0.12.20260815",
    "pyarrow-stubs>=20.0.0.20260625",
    "pycrdt>=0.14.2",
    "hypothesis>=6.165.10",
    "psycopg[binary]>=3.3.4",
]

[tool.setuptools.package-data]
"polars_hist_db" = ["py.typed"]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q -m 'not integration'"
log_cli = "true"
log_cli_level = "WARN"
markers = [
    "integration: needs external infra (MySQL / NATS); excluded from default collection. Run via `pytest -m integration` in a workflow that brings the infra up.",
]
# log_cli_level = "INFO"
log_format = "%(asctime)s %(levelname)s %(message)s"
log_date_format = "%Y-%m-%d %H:%M:%S"
testpaths = [
    "tests"
]

[tool.uv.sources]
quarto-cli = { git = "https://github.com/quarto-dev/quarto-cli", rev = "abc6a78ed68f9e8bc9d54e27851093bd687a1cb7" }

[tool.mypy]
warn_unused_configs = true
plugins = ["sqlalchemy.ext.mypy.plugin"]
packages = ["polars_hist_db"]

[[tool.mypy.overrides]]
module = [
    "sql_metadata.*",
    "scandir_rs.*",
    "nats.*",
    "nkeys.*",
    "adbc_driver_flightsql.*",
    "psycopg.*",
    "pyarrow.*",
    "opentelemetry.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
disallow_untyped_decorators = false

[tool.ruff]
extend = ".shared/ruff.toml"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
